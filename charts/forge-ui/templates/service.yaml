apiVersion: v1
kind: Service
metadata:
  name: rh-forge-ui
  labels:
    app.kubernetes.io/name: rh-forge-ui
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  selector:
    app.kubernetes.io/name: rh-forge-ui
  ports:
    - name: http
      port: 8888
      targetPort: 8888
      protocol: TCP
